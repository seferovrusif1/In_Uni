namespace arrays
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int a = 32;
            int[] nums = { 12, 95, a, 96, 76 };
            //TASK 1 cut indeksli ededler
            
            //for (int i = 0; i < nums.Length; i++)
            //{
            //    if (i%2==0)
            //    {
            //        Console.WriteLine(nums[i]);
            //    }

            //}

            //TASK 2 cut ededler


            //for (int i = 0; i < nums.Length; i++)
            //{
            //    if (nums[i] % 2 == 0)
            //    {
            //        Console.WriteLine(nums[i]);
            //    }

            //}

            // TASK 3 5e bolunen sayi

            //int say = 0;
            //for (int i = 0; i < nums.Length; i++)
            //{
            //    if (nums[i] % 5 == 0)
            //    {
            //        say++;
            //    }
            //}
            //Console.WriteLine(say);


            // TASK 4

            //int bignum = nums[0];

            //for (int i = 1; i < nums.Length; i++)
            //{
            //    if (nums[i] > bignum)
            //    {
            //        bignum = nums[i];
            //        index = i;
            //    }
            //}
            //Console.WriteLine(index);


            ///

            //int bignumI = 0;


            //for (int i = 1; i < nums.Length; i++)
            //{
            //    if (nums[i] > nums[bignumI])
            //    {
            //        bignumI = i;

            //    }

            //}
            //Console.WriteLine(bignumI);
        }
    }
}