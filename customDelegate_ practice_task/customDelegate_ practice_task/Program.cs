using customDelegate__practice_task.Models;

namespace customDelegate__practice_task
{
    internal class Program
    {
        static void Main(string[] args)
        {
            DelegateMethods meth = new DelegateMethods();
            //bool iscontinue=true;
            //while (iscontinue)
            //{
            meth.methods(meth.methods(Convert.ToInt32(Console.ReadLine()), Convert.ToInt32(Console.ReadLine()), customDelegate meth.Multiply ));
                Console.WriteLine("1-Multiply   2-Sum   3-Substract");
                string a = Console.ReadLine();

                switch (a)
                {
                    case "1":

                        Console.WriteLine(meth.methods(Convert.ToInt32(Console.ReadLine()), Convert.ToInt32(Console.ReadLine()),Multiply);
                        break;
                }
            
        }
    }
}